Commands for this project...

ALIASES (e.g. ~/.oh-my-zsh/custom/aliases.zsh)

wie

	Change into project's directory

WP CONTINUOUS DEPLOYMENT

Deploy from GitHub to WordPress.org SVN with GitHub Actions
https://github.com/ahmadawais/wp-continuous-deployment

Install: npx wp-continuous-deployment (requires npm)

When on master branch...

git push

	Readme and assets deployed if no other changes made...

git tag -a #.# -m "Message";
git push --tags;

	New version deployed to WordPress.org
