jQuery( document ).ready( function( $ ) {

	/* MAY NOT NEED THIS */

	/**
	 * Redirect clicks on "Export Widgets"
	 */
	/*
	$( '#wie-export-button' ).click( function() {

		var export_url;

		export_url = 'http://google.com';

		window.location = export_url;

	} );
	*/

} );